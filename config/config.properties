# Kafka Producer Performance Test Configuration

# Test Parameters
numOfRecords=70000
recordSize=4000
throughputRate=20

# Topic Configuration
topicPrefix=benchmark-topic-
topicstart=1
topicend=10

# Kafka Configuration
kafkaHome=/opt/kafka
bootstrapServers=localhost:9092
producerConfig=config/producer.config

# Elasticsearch Configuration
elasticsearchHost=localhost
elasticsearchPort=9200
indexname=kafka-producer-benchmark

# Results will be indexed to Elasticsearch for analysis